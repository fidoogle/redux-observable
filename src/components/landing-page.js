import React from 'react';
import Login from 'login';

//This can point to any page. For now it points to the login page
const LandingPage = () => {
    return (
        <Login/>
    );
};

export default LandingPage;